<ul>
  <% @restaurants.each do |restaurant| %>
    <li>
    <%= restaurant.name %> <%= restaurant.address %> <%= link_to "See details", restaurant_path(restaurant), class: "btn btn-primary"%>
  </li>
  <br>
  <%end%>
</ul>
<h1> <%= link_to "Create new restaurant", new_restaurant_path(@task) %> </h1>
